{
  "name": "troll-hunterz",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "yarn --cwd server start & yarn --cwd client start",
    "deploy": "yarn --cwd server deploy & yarn --cwd client deploy",
    "install": "yarn --cwd server install & yarn --cwd client install"
  }
}
