{
  "name": "troll-hunterz",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "concurrently --names \"SERVER,CLIENT\" -c \"bgGreen.bold,bgMagenta.bold\" \"npm run start --prefix server\" \"npm run start --prefix client\"",
    "deploy": "npm --prefix server deploy & npm --prefix client deploy",
    "install": "npm --prefix server install & npm --prefix client install"
  }
}
