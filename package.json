{
  "name": "troll-hunterz",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "concurrently --names \"SERVER,CLIENT\" -c \"bgGreen.bold,bgMagenta.bold\" \"yarn --cwd server start\" \"yarn --cwd client start\"",
    "deploy": "npm --prefix server deploy & npm --prefix client deploy",
    "install": "npm --prefix server install & npm --prefix client install"
  }
}
