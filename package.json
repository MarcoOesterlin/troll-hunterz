{
  "name": "troll-hunterz",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "npm --cwd server start & npm --cwd client start",
    "deploy": "npm --cwd server deploy & npm --cwd client deploy",
    "install": "npm --cwd server install & npm --cwd client install"
  }
}
